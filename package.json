{
  "name": "agent-skills",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "validate": "bun run typecheck && bun test && bun run scripts/validate.ts"
  },
  "engines": {
    "bun": ">=1.3.13 <2"
  },
  "devDependencies": {
    "@types/bun": "1.3.5",
    "typescript": "5.9.3",
    "undici-types": "7.16.0"
  }
}
